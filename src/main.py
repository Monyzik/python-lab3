from algorithms.factorial import factorial


# app = typer.Typer()

def main() -> None:
    """
    Обязательнная составляющая программ, которые сдаются. Является точкой входа в приложение
    :return: Данная функция ничего не возвращает
    """
    # typer.echo("Welcome to the interactive Typer session!")
    # while (user_input := typer.prompt("Enter a command (or 'quit' to exit)")) != "quit":
    #     if user_input.lower() == "hello":
    #         typer.echo("Hello there!")
    #     elif user_input.lower() == "info":
    #         typer.echo("This is an example of an interactive loop with Typer.")
    #     else:
    #         # typer.
    #         typer.echo(f"Unknown command: '{user_input}'")
    pass


if __name__ == "__main__":
    main()
